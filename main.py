import requests; response = requests.post("https://api.together.xyz/v1/chat/completions", headers={"Authorization": f"Bearer {'5139f0130d09b2c6e029b06996f5e8224d8657acd85e855aae71a204b81b5b4a'}", "Content-Type": "application/json"}, json={"model": "meta-llama/Meta-Llama-3.1-8B-Instruct-Turbo", "messages": [{"role": "user", "content": f"Act like a trainee developer. Write me the shittiest and the stupidest solution. Write a code that generates a completely random number from 1 to 100... but without a random number generator. You should return me only the code. The result should be in one line. Its not a functions, just code to run. In print"}]}); print('Code:', response.json()['choices'][0]['message']['content']); exec(response.json()['choices'][0]['message']['content'])
