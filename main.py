import requests, string, random; exec('while True:\n try:\n  response = requests.post("https://api.together.xyz/v1/chat/completions", headers={"Authorization": f"Bearer 5139f0130d09b2c6e029b06996f5e8224d8657acd85e855aae71a204b81b5b4a", "Content-Type": "application/json"}, json={"model": "meta-llama/Meta-Llama-3.1-405B-Instruct-Turbo", "messages": [{"role": "user", "content": f"Act like a trainee developer. Write me the shittiest and the stupidest solution. Write a code that generates a completely random number from 1 to 100 but without a random number generator. You should return me only the code. The code solution should be in one line. Its not a functions, just code to run. In print. Code should return number from 1 to 100 and all. If number is more than 100, rewrite the code. Dont mention this letters:{random.choice(string.ascii_letters)}"}]}); print(response.json()[\'choices\'][0][\'message\'][\'content\'].replace(\'`\', \'\')); exec(response.json()[\'choices\'][0][\'message\'][\'content\'].replace(\'`\', \'\'))\n  break\n except Exception as e: pass')